name: custom_lint_builder
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  analyzer: ^3.3.1
  analyzer_plugin: ^0.9.0
  # TODO use tight constraints
  custom_lint:
    path: ../custom_lint
  package_config: ^2.0.2
  path: ^1.8.1
  pub_semver: ^2.1.1
  pubspec_parse: ^1.2.0
  recase: ^4.0.0
  source_span: ^1.8.2
